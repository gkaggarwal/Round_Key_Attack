#ifndef AES_ROUND_REVERSAL_H
#define AES_ROUND_REVERSAL_H

#include <iostream>
#include <vector>
#include <string>
#include <sstream>
#include <iomanip>

using namespace std;

const uint8_t inverseSbox[256] = {
    
};

void inverseShiftRows(uint8_t state[4][4]) {
    uint8_t temp;

    // Row 1: rotate right by 1
    temp = state[1][3];
    state[1][3] = state[1][2];
    state[1][2] = state[1][1];
    state[1][1] = state[1][0];
    state[1][0] = temp;

    // Row 2: rotate right by 2
    temp = state[2][0];
    state[2][0] = state[2][2];
    state[2][2] = temp;
    temp = state[2][1];
    state[2][1] = state[2][3];
    state[2][3] = temp;

    // Row 3: rotate right by 3 (or left by 1)
    temp = state[3][0];
    state[3][0] = state[3][1];
    state[3][1] = state[3][2];
    state[3][2] = state[3][3];
    state[3][3] = temp;
}

uint8_t gmul(uint8_t a, uint8_t b) {
    uint8_t p = 0;
    for (int i = 0; i < 8; ++i) {
        if (b & 1) p ^= a;
        bool hi_bit_set = (a & 0x80);
        a <<= 1;
        if (hi_bit_set) a ^= 0x1B; // x^8 + x^4 + x^3 + x + 1
        b >>= 1;
    }
    return p;
}

void inverseMixColumns(uint8_t state[4][4]) {
    uint8_t temp[4];
    for (int c = 0; c < 4; ++c) {
        temp[0] = gmul(state[0][c], 0x0E) ^ gmul(state[1][c], 0x0B) ^ gmul(state[2][c], 0x0D) ^ gmul(state[3][c], 0x09);
        temp[1] = gmul(state[0][c], 0x09) ^ gmul(state[1][c], 0x0E) ^ gmul(state[2][c], 0x0B) ^ gmul(state[3][c], 0x0D);
        temp[2] = gmul(state[0][c], 0x0D) ^ gmul(state[1][c], 0x09) ^ gmul(state[2][c], 0x0E) ^ gmul(state[3][c], 0x0B);
        temp[3] = gmul(state[0][c], 0x0B) ^ gmul(state[1][c], 0x0D) ^ gmul(state[2][c], 0x09) ^ gmul(state[3][c], 0x0E);
        for (int i = 0; i < 4; ++i)
            state[i][c] = temp[i];
    }
}

// Inverse SubBytes
void inverseSubBytes(uint8_t state[4][4]) {
    for (int r = 0; r < 4; ++r) {
        for (int c = 0; c < 4; ++c) {
            state[r][c] = inverseSbox[state[r][c]];
        }
    }
}

// Helper: Convert 32-char hex string to 4x4 matrix
void stringToState(const string& hex, uint8_t state[4][4]) {
    for (int i = 0; i < 16; ++i) {
        string byteStr = hex.substr(i * 2, 2);
        state[i % 4][i / 4] = static_cast<uint8_t>(strtol(byteStr.c_str(), nullptr, 16));
    }
}

// Helper: Convert 4x4 matrix back to hex string
string stateToString(uint8_t state[4][4]) {
    stringstream ss;
    ss << uppercase;
    for (int c = 0; c < 4; ++c) {
        for (int r = 0; r < 4; ++r) {
            ss << hex << setw(2) << setfill('0') << (int)state[r][c];
        }
    }
    return ss.str();
}



// Full Inverse Round function (with AddRoundKey first)
string inverseRoundWithAddRoundKey(const string& hexInput, const string& hexKey) {
    uint8_t state[4][4];
    uint8_t roundKey[4][4];

    stringToState(hexInput, state);
    stringToState(hexKey, roundKey);

    // AddRoundKey
    for (int r = 0; r < 4; ++r) {
        for (int c = 0; c < 4; ++c) {
            state[r][c] ^= roundKey[r][c];
        }
    }

    //inverseMixColumns(state);
    inverseShiftRows(state);
    inverseSubBytes(state);

    return stateToString(state);
}

#endif // AES_KEY_REVERSAL_H
